export const incrFunc = () => {
    return { type: "INC" };
};

export const decrFunc = () => {
    return { type: "DEC" };
};

export const resetFunc = () => {
    return { type: "RESET" };
};

// export default incrFuncdecrFunc,resetFunc;
